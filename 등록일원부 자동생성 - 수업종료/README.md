# 등록일원부 자동생성 - 수업종료

> **Category:** Math4U  
> **Workflow ID:** `BrZ88oo3KRKccX0g`  
> **노션 메뉴얼:** https://www.notion.so/323c6a06e17e81b6b0a5d91342537f5c  
> **자동 생성:** Gemini AI  

---

# 등록일원부 자동생성 - 수업종료 워크플로우

본 워크플로우는 Math4U 시스템에서 특정 수업이 종료되었을 때, 이를 감지하여 자동으로 등록일원부 레코드를 생성하는 n8n 자동화 프로세스입니다.

## 📋 워크플로우 개요
- **목적**: `Student_Study` 데이터베이스에서 수업이 완료된 경우, 해당 학생의 수업료와 납부 회차를 계산하여 '등록일원부'에 자동으로 기록을 생성합니다.
- **카테고리**: Math4U

## 🔄 전체 흐름
1. **Notion Trigger**: `Student_Study` 데이터베이스의 변경 사항을 감지합니다.
2. **Code (종료 체크)**: '수업완료' 체크박스 확인 및 수업 회차 일치 여부를 검증합니다.
3. **Notion (Class 조회)**: 해당 수업의 수업료 정보를 가져옵니다.
4. **Code (수업료 추출)**: 조회된 데이터에서 수업료를 파싱합니다.
5. **Notion (Students 조회)**: 학생의 이름을 가져옵니다.
6. **Code (이름 병합)**: 학생 이름과 이전 데이터를 병합합니다.
7. **HTTP (납부회차 조회)**: 기존 등록일원부 데이터를 조회하여 현재 납부 회차를 계산합니다.
8. **Code (납부회차 계산)**: 연도별 누적 회차를 계산하여 새로운 `paySeq`를 생성합니다.
9. **HTTP (레코드 생성)**: 최종 데이터를 '등록일원부' 데이터베이스에 저장합니다.

## ⚙️ 설정 및 요구사항
- **Notion Credential**: Notion API 접근 권한이 있는 크레덴셜이 설정되어 있어야 합니다.
- **데이터베이스 ID**:
    - `Student_Study` DB ID: `2d0c6a06e17e81188ce1dd2f76406aa1`
    - `등록일원부` DB ID: HTTP 노드 내 URL에 포함된 ID 확인 필요

## 🔧 수정 및 유지보수
- **수업료 조회 조건**: `Notion - Class 수업료 조회` 노드에서 참조하는 속성 확인
- **납부회차 로직**: `Code - 납부회차 계산` 노드 내 연도별 회차 증가 로직 수정 가능
- **데이터베이스 연결**: `HTTP - 등록일원부 레코드 생성` 노드의 URL 및 JSON Body 구조 확인

## ⚠️ 주의사항
- **데이터 형식**: `Student_Study` DB의 '수업회차'와 '수업구분' 속성 형식이 변경될 경우 `Code - 종료 체크 및 데이터 추출` 노드의 정규식 및 파싱 로직을 업데이트해야 합니다.
- **API 버전**: Notion API 버전은 `2022-06-28`을 기준으로 작성되었습니다.
- **에러 처리**: 데이터가 누락되거나 관계형 데이터가 연결되지 않은 경우 워크플로우가 중단될 수 있으므로, 각 단계의 데이터 유효성 검사를 유지하십시오.